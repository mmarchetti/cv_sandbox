
This repository contains some helper functions for use with OpenCV.

The Window class allows displaying multiple images, with features
like click-to-zoom and a color info panel.

The Camera class is a wrapper for acquiring images from gstreamer's v4l2src.
